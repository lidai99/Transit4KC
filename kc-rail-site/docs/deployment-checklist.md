# Deployment checklist

1. Upload the contents of the `kc-rail-site` folder to your hosting provider.
2. Confirm `/` redirects to `/waldo-extension/`.
3. Confirm the Waldo Extension PDF pages scroll vertically.
4. Confirm `/voice-your-support/` displays the embedded Google Form.
5. Confirm the fallback Google Form link opens in a new tab.
